# ePIC tracking performance with and without beam-related background using agent 
Used opencode with claude for planning and part of the building. Majority of the workload is done by big-pickle which is free. Note: the results and code from agents has yet to be checked. 

To run this in another agent flow, the PLAN.md is the starting point. Just ask the agent to digest the PLAN.md and move on . 

to continue the agent flow after it stopped, update continue.sh to give instruction to the agent to continue. Note: continue.sh should be run outside of eic-shell
